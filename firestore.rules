// firestore.rules
// כל משתמש מחובר יכול לקרוא ולכתוב — אפליקציה פרטית למשפחה

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
